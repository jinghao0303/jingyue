# Python 知识点全景梳理

## 一、 基础语法与流程控制

### 1. 变量与运算符

- **布尔表达式**：if 和 while 的条件必须是布尔表达式（或能隐式转换为布尔值的对象）。
  - *False的情况*：False, 0, None, 空字符串 "", 空列表 [], 空字典 {} 等。
- **逻辑运算**：and (与), or (或), not (非)。
- **比较运算**：
  - ==：判断**值**是否相等。
  - is：判断是否是同一个**对象**（内存地址是否相同）。
- **类型转换**：int() 转整数，str() 转字符串。

### 2. 条件语句

- **结构**：if ... elif ... else ...。
- **规则**：
  - if 可以单独使用。
  - elif 可以有多个，必须跟在 if 后。
  - else 是可选的，处理所有未匹配的情况。
  - 支持嵌套。

### 3. 循环语句

- **for 循环**：用于遍历序列（列表、元组、字符串等）或可迭代对象。
- **while 循环**：当条件为 True 时重复执行。
- **循环控制关键字**：
  - **break**：**终止**整个循环，跳出循环体。
  - **continue**：**跳过**本次循环剩余代码，直接进入下一次循环判断。

## 二、 数据结构 (容器)

### 1. 字符串 (String)

- 
- **特性**：**不可变**类型，有序。
- **操作**：
  - 
  - 拼接：使用 + 运算符。
  - 长度：使用 **len()** 函数（注意不是 length）。
- **常用方法**：
  - 
  - lower()：转小写。
  - startswith(prefix)：判断是否以指定字符串开头。
  - replace(old, new)：替换子串（**注意**：不会修改原字符串，而是返回新字符串）。

### 2. 列表 (List)

- **特性**：**可变**类型，有序，允许重复元素。
- **推导式**：[表达式 for 变量 in 序列 if 条件]。
- **常用方法**：
  - append(x)：在**末尾**添加元素。
  - insert(index, x)：在**指定位置**插入元素。
  - pop(index)：删除并返回指定位置的元素（默认删最后一个）。
- **转换**：list()。

### 3. 元组 (Tuple)

- **特性**：**不可变**类型，有序。一旦创建，不能修改元素。
- **转换**：tuple()。

### 4. 字典 (Dictionary)

- **特性**：**可变**类型，键值对集合 {key: value}。
  - **键 (Key)**：必须是**不可变**类型（如字符串、数字、元组），不能是列表。
  - **无序性**：题目考点中通常认为字典是无序的（尽管Python 3.7+内部实现有序）。
- **推导式**：{k: v for k, v in 序列}。
- **操作**：
  - 添加/修改：dict[key] = value。
  - 获取值：dict.get(key)（键不存在返回 None，不会报错）。
  - 删除：dict.pop(key)（删除指定键并返回值），dict.clear()（清空所有）。

### 5. 集合 (Set)

- **特性**：**无序**，**不重复**（自动去重）。
- **操作**：
  - 添加：add(x)。
  - 删除：remove(x)（元素不存在会报错）。
- **用途**：去重、集合运算（交集、并集等）。

## 三、 函数 (Function)

### 1. 定义与调用

- **定义**：使用 **def** 关键字。
- **返回值**：使用 **return**。如果没有 return，默认返回 None。
- **参数类型**：
  - **位置参数**：按顺序传递。
  - **关键字参数**：func(a=1)，调用时顺序可变，但必须放在位置参数之后。
  - **默认参数**：def func(a, b=1)，定义时必须放在非默认参数之后。
  - **可变参数**：
    - *args：接收任意个**位置**参数，打包成**元组**。
    - **kwargs：接收任意个**关键字**参数，打包成**字典**。

### 2. Lambda 表达式

- **定义**：匿名函数，格式 lambda 参数: 表达式。
- **限制**：只能包含**一条表达式**，不能包含复杂逻辑块。

### 3. 作用域 (Scope)

- **局部变量**：定义在函数内部，只能在内部访问。
- **全局变量**：定义在函数外部，全程序可见。
- **关键字 global**：在函数内部**修改**全局变量时，必须先声明 global 变量名。

### 4. 高阶函数

- **map(func, seq)**：将函数应用到序列的每个元素上。
- **filter(func, seq)**：筛选出满足条件的元素。

## 四、 面向对象编程 (OOP)

### 1. 类与对象

- **定义类**：class 类名:。
- **实例化**：对象名 = 类名()。
- **self**：代表**当前对象的引用**（实例本身）。定义实例方法时，第一个参数必须是 self，调用时**不需要**显式传递。

### 2. 构造与析构

- **构造函数**：**__init__**，用于初始化对象属性。
- **析构函数**：**__del__**，对象销毁时调用。
- **字符串表示**：
  - __str__：定义对象的字符串描述（用于 print）。
  - __repr__：用于控制对象的官方字符串表现形式。

### 3. 继承

- **语法**：class 子类(父类):。
- **作用**：代码复用、扩展功能。
- **重写 (Override)**：子类定义与父类**同名**的方法，覆盖父类行为。
- **访问**：子类可以继承并访问父类的属性和方法。

### 4. 方法分类

- **实例方法**：第一个参数是 self，通过对象调用。
- **类方法**：使用 **@classmethod** 装饰，第一个参数是 **cls**，可以访问类属性。
- **静态方法**：使用 **@staticmethod** 装饰，没有 self 或 cls，不能访问实例属性。

## 五、 异常处理 (Exception)

### 1. 结构

codePython

```
try:
    # 可能引发异常的代码
except 异常类型:
    # 捕获并处理异常
else:
    # 如果没有发生异常执行
finally:
    # 无论是否发生异常，都会执行
```

### 2. 细节规则

- **捕获多个异常**：except (TypeError, ValueError): （必须用元组/小括号包裹）。
- **引发异常**：使用 **raise** 关键字。
- **常见异常**：ZeroDivisionError (除零错误)。

## 六、 文件操作 (File I/O)

### 1. 基本操作

- **打开**：f = open(路径, 模式)。
- **关闭**：f.close()。
- **上下文管理**：with open(...) as f:，代码块结束自动关闭文件。

### 2. 打开模式

- **'r'**：**只读**（默认）。
- **'w'**：**写入**。如果文件存在，**覆盖**（清空）原内容；不存在则创建。
- **'a'**：**追加**。写在文件末尾，不存在则创建。

### 3. 读写方法

- **read()**：读取**全部**内容（返回字符串）。
- **readline()**：读取**一行**。
- **readlines()**：读取所有行，返回一个**列表**。
- **write(str)**：写入字符串。

## 七、 模块 (Module)

### 1. 导入方式

- import 模块名
- from 模块名 import 函数名
- import 模块名 as 别名

### 2. 特殊变量 __name__

- 作用：存储模块的名称。
- **if __name__ == 'main':**：判断模块是**被直接运行**（值为 'main'）还是**被导入**（值为模块名）。只有直接运行时，该语句块内的代码才执行。